var import_crypto = require("crypto");,var rnds8Pool = new Uint8Array(256),
  poolPtr = rnds8Pool.length;,function rng() {
  return poolPtr > rnds8Pool.length - 16 && ((0, lQe.randomFillSync)(rnds8Pool), poolPtr = 0), rnds8Pool.slice(poolPtr, poolPtr += 16);
},__name(rng, "rng");