var require_util = __commonJSMin((exports, module) => {
  "use strict";

  var assert = require("assert"),
    {
      URLSerializer: URLSerializer
    } = zA(),
    {
      isValidHeaderName: isValidHeaderName
    } = Lu();
  function urlEquals(A, B, excludeFragment = !1) {
    let serializedA = URLSerializer(A, excludeFragment),
      serializedB = URLSerializer(B, excludeFragment);
    return serializedA === serializedB;
  }
  __name(urlEquals, "urlEquals");
  function getFieldValues(header) {
    assert(header !== null);
    let values = [];
    for (let value of header.split(",")) value = value.trim(), isValidHeaderName(value) && values.push(value);
    return values;
  }
  __name(getFieldValues, "getFieldValues");
  module.exports = {
    urlEquals: urlEquals,
    getFieldValues: getFieldValues
  };
});