var require_retry = __commonJSMin((exports, module) => {
  "use strict";

  var RetryHandler = NM();
  module.exports = globalOpts => dispatch => __name(function (opts, handler) {
    return dispatch(opts, new RetryHandler({
      ...opts,
      retryOptions: {
        ...globalOpts,
        ...opts.retryOptions
      }
    }, {
      handler: handler,
      dispatch: dispatch
    }));
  }, "retryInterceptor");
});