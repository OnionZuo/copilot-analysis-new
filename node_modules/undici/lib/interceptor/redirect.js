var require_redirect = __commonJSMin((exports, module) => {
  "use strict";

  var RedirectHandler = CM();
  module.exports = opts => {
    let globalMaxRedirections = opts == null ? void 0 : opts.maxRedirections;
    return dispatch => __name(function (opts, handler) {
      let {
        maxRedirections = globalMaxRedirections,
        ...baseOpts
      } = opts;
      if (!maxRedirections) return dispatch(opts, handler);
      let redirectHandler = new RedirectHandler(dispatch, maxRedirections, opts, handler);
      return dispatch(baseOpts, redirectHandler);
    }, "redirectInterceptor");
  };
});