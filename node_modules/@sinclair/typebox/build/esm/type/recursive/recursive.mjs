var Ordinal = 0;,function Recursive(callback, options = {}) {
  IsUndefined(options.$id) && (options.$id = `T${Ordinal++}`);
  let thisType = CloneType(callback({
    [Kind]: "This",
    $ref: `${options.$id}`
  }));
  return thisType.$id = options.$id, CreateType({
    [Hint]: "Recursive",
    ...thisType
  }, options);
},__name(Recursive, "Recursive");