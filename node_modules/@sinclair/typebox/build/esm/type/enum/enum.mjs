function Enum(item, options) {
  if (IsUndefined(item)) throw new Error("Enum undefined or empty");
  let values1 = globalThis.Object.getOwnPropertyNames(item).filter(key => isNaN(key)).map(key => item[key]),
    anyOf = [...new Set(values1)].map(value => Literal(value));
  return Union(anyOf, {
    ...options,
    [Hint]: "Enum"
  });
},__name(Enum, "Enum");