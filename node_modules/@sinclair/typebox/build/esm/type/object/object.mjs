function RequiredKeys(properties) {
  let keys = [];
  for (let key in properties) IsOptional(properties[key]) || keys.push(key);
  return keys;
},__name(RequiredKeys, "RequiredKeys");,function _Object(properties, options) {
  let required = RequiredKeys(properties),
    schematic = required.length > 0 ? {
      [Kind]: "Object",
      type: "object",
      properties: properties,
      required: required
    } : {
      [Kind]: "Object",
      type: "object",
      properties: properties
    };
  return CreateType(schematic, options);
},__name(_Object, "_Object");,var Object = _Object;