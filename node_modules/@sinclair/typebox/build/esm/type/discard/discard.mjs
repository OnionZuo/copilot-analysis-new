function DiscardKey(value, key) {
  let {
    [key]: _,
    ...rest
  } = value;
  return rest;
},__name(DiscardKey, "DiscardKey");,function Discard(value, keys) {
  return keys.reduce((acc, key) => DiscardKey(acc, key), value);
},__name(Discard, "Discard");