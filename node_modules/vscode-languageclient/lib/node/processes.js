var require_processes = __commonJSMin(exports => {
  "use strict";

  Object.defineProperty(exports, "__esModule", {
    value: !0
  });
  exports.terminate = void 0;
  var cp = require("child_process"),
    path_1 = require("path"),
    isWindows = process.platform === "win32",
    isMacintosh = process.platform === "darwin",
    isLinux = process.platform === "linux";
  function terminate(process, cwd) {
    if (isWindows) try {
      let options = {
        stdio: ["pipe", "pipe", "ignore"]
      };
      return cwd && (options.cwd = cwd), cp.execFileSync("taskkill", ["/T", "/F", "/PID", process.pid.toString()], options), !0;
    } catch {
      return !1;
    } else if (isLinux || isMacintosh) try {
      var cmd = (0, path_1.join)(__dirname, "terminateProcess.sh"),
        result = cp.spawnSync(cmd, [process.pid.toString()]);
      return !result.error;
    } catch {
      return !1;
    } else return process.kill("SIGKILL"), !0;
  }
  __name(terminate, "terminate");
  exports.terminate = terminate;
});